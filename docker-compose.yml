version: '3.8'

services:
  abc-helper:
    image: nginx:alpine
    container_name: abc-helper-app
    restart: always
    ports:
      - "8080:80"
    environment:
      - API_KEY=${API_KEY}
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./sounds:/usr/share/nginx/html/sounds:ro