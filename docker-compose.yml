version: '3.8'

services:
  abc-helper:
    image: nginx:alpine
    container_name: abc-helper-app
    restart: always
    ports:
      - "9090:80"
    environment:
      - API_KEY=${API_KEY}
    volumes:
      # rw (read-write) дозволяє nginx створити папку sounds всередині для наступного монтування
      - ./dist:/usr/share/nginx/html:rw
      - ./sounds:/usr/share/nginx/html/sounds:ro