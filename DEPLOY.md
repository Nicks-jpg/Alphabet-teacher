# Деплой проекту Абетка (ABC Helper) на Ubuntu за допомогою Portainer

Цей файл містить покрокову інструкцію, як розгорнути цей проект на вашому сервері Ubuntu, використовуючи Docker та Portainer.

## Передумови

1.  **Сервер Ubuntu** з встановленим Docker та Docker Compose.
2.  **Portainer**, встановлений та запущений.
3.  **API Key** від Google Gemini (ТІЛЬКИ для перевірки малюнків у 3-му режимі).

---

## 📂 Папка зі звуками (ОБОВ'ЯЗКОВО!)

Оскільки ми відмовилися від генерації звуку через АІ, вам потрібно завантажити MP3 файли для кожної літери.

1.  На сервері створіть папку `sounds` у директорії проекту (або змонтуйте її в Portainer).
2.  Файли мають називатися кириличними великими літерами.
    *   Приклад: `А.mp3`, `Б.mp3`, `В.mp3` ... `Я.mp3`.
3.  Якщо файлу не буде знайдено, програма спробує використати вбудований у браузер синтезатор мови (роботизований голос).

---

## Варіант 1: Деплой через GitHub (Stacks) - Рекомендовано

1.  Зайдіть у **Portainer**.
2.  Перейдіть у **Stacks** -> **Add stack**.
3.  Оберіть **Repository**.
4.  Заповніть поля:
    *   **Name:** `abc-helper`.
    *   **Repository URL:** `https://github.com/Nicks-jpg/Alphabet-teacher`.
    *   **Repository reference:** `refs/heads/main` (або `final-fix-v3`).
    *   **Compose path:** `docker-compose.yml`.
5.  У розділі **Environment variables** додайте:
    *   `API_KEY`: Ваш ключ від Google Gemini.
6.  Натисніть **Deploy the stack**.

---

## Варіант 2: Локальний деплой

Якщо ви клонуєте код вручну:

1.  Покладіть MP3 файли у папку `public/sounds/`.
2.  Створіть `.env` файл з `API_KEY=...`.
3.  Запустіть:
    ```bash
    docker-compose up -d --build
    ```

## Перевірка роботи

1.  Відкрийте: `http://IP-адреса:3030`
2.  Перевірте звук: `http://IP-адреса:3030/test-sound.html` (тепер перевіряє завантаження MP3).
